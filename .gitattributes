# ── 换行符 ───────────────────────────────────────────────────────────
# 只对「必须 LF 才能运行」的文件做强制。钩子若是 CRLF，sh 会直接报错。
.githooks/*   text eol=lf
*.sh          text eol=lf
Dockerfile    text eol=lf
*.Dockerfile  text eol=lf

# Windows 脚本保持 CRLF
*.ps1         text eol=crlf
*.bat         text eol=crlf
*.cmd         text eol=crlf

# 说明：未在此处声明的文本文件不做换行规范化。
# 当前仓库里 .py 有 86 个 CRLF / 67 个 LF 混用，
# 统一换行会产生上百文件的白空格差异，应另择时机作为独立提交处理，
# 不要夹带在功能提交里。

# ── 二进制（禁止换行转换与文本 diff）────────────────────────────────
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.webp  binary
*.docx  binary
*.xlsx  binary
*.pdf   binary
*.db    binary
*.exe   binary
*.dll   binary
*.pyd   binary
*.so    binary
*.zip   binary
*.zstd  binary
